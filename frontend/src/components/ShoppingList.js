import React, { useEffect, useState } from 'react';
import { Link } from 'react-router-dom';
import api from '../api';

const ShoppingList = ({recipes, ShoppingList}) => {
    
    return(
      <h1>Shopping List!</h1>
    );
};

export default ShoppingList;
